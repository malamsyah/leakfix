# aws-leak fixture

Contains a single file with the documented AWS example access key ID.
Used by integration tests as a known-finding fixture.

The string is **not a real credential**: it is `AKIAIOSFODNN7EXAMPLE`, the
AWS-published example value used in IAM tutorials.
