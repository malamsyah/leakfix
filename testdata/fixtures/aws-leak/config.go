package config

// FAKE TEST FIXTURE — not a real key. The string below uses AWS's documented
// example access key ID, which is widely published in AWS IAM tutorials and
// is not, and never was, a live credential.
var DefaultAccessKey = "AKIAIOSFODNN7EXAMPLE"
